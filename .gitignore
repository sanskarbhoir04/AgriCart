# ── Secrets — never commit ──
.env

# ── OS / editor junk ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── PHP / logs ──
*.log
error_log

# ── Uploaded user content (keep the folder, not its contents) ──
assets/uploads/*
!assets/uploads/.gitkeep
